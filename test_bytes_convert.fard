import("std/io") as io
import("std/bytes") as bytes
import("std/csv") as csv

fn main() {
  let raw = io.read_file("data/pubchem_periodic_table.csv") in
  let text = bytes.to_str(raw) in
  let parsed = csv.parse(text) in
  "ok"
}
main()
