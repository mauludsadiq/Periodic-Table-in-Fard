import("std/json") as json
import("std/io") as io

fn main() {
  let my_record = { "name": "Hydrogen", "number": 1 }
  let encoded = json.encode(my_record)
  io.write_file("out/test.json", encoded)
}
main()
